AI Chat Bot with LangChain + Gradio

Goal: Use LangChain & OpenAI LLM module to call OpenAI and display response.

Tools: LangChain + Gradio + OpenAI API

What you’ll build: A Gradio chat UI that accepts user prompt and returns LLM response.

Concepts:

LangChain LLMChain

Prompt templates

Gradio text inputs and outputs
